# LifeOS web

Reserved for the React + TypeScript + Vite client. Bootstrap only through the assigned Epic 02 ticket. Production base path is `/life-os/` and protected routes begin at `/life-os/app/`.

