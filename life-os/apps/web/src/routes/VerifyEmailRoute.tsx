import { VerifyEmailScreen } from "@features/auth";

/** VerifyEmailRoute (LOS-0603). Mounts LOS-0511's `VerifyEmailScreen` at `/life-os/verify-email`. */
export function VerifyEmailRoute() {
  return <VerifyEmailScreen />;
}
