import { ResetPasswordScreen } from "@features/auth";

/** ResetPasswordRoute (LOS-0603). Mounts LOS-0512's `ResetPasswordScreen` at `/life-os/reset-password`. */
export function ResetPasswordRoute() {
  return <ResetPasswordScreen />;
}
