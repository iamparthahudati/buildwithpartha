# Public browser configuration only. Copy to .env.local for local development.
# Never add secrets to a VITE_* value because Vite includes it in the browser bundle.
VITE_APP_BASE_PATH=/life-os/
VITE_API_BASE_PATH=/life-os/api/v1
