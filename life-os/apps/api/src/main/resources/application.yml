spring:
  application:
    name: life-os-api
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/lifeos}
    username: ${DATABASE_USERNAME:lifeos}
    password: ${DATABASE_PASSWORD:replace-locally}
    hikari:
      pool-name: LifeOsPool
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  mail:
    host: ${SMTP_HOST:localhost}
    port: ${SMTP_PORT:1025}
    username: ${SMTP_USERNAME:}
    password: ${SMTP_PASSWORD:}

server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: /life-os/api/v1
  forward-headers-strategy: framework

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: never
