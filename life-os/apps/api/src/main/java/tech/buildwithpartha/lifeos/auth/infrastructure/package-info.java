/**
 * Adapters for {@code auth.domain} ports: Argon2id hashing, the common-password wordlist, JPA
 * persistence for the identity schema ({@code V2__identity_schema.sql}), secure token generation,
 * and the signup rate limiter.
 */
package tech.buildwithpartha.lifeos.auth.infrastructure;
