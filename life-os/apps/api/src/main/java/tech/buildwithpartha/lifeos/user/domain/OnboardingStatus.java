package tech.buildwithpartha.lifeos.user.domain;

/** State of a user's onboarding journey (LOS-0513, 25-ONBOARDING-SPECIFICATION.md). */
public enum OnboardingStatus {
  NOT_STARTED,
  IN_PROGRESS,
  COMPLETED
}
