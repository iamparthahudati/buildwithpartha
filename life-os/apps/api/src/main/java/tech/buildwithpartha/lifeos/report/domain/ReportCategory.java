package tech.buildwithpartha.lifeos.report.domain;

/** Category domain grouping for named report definitions (LOS-1109). */
public enum ReportCategory {
  TASKS,
  TIME,
  PROJECTS,
  GOALS,
  REVIEWS,
  ANALYTICS
}
