package tech.buildwithpartha.lifeos.task.domain;

/** Scope selector for editing recurring tasks and occurrences. */
public enum RecurrenceEditScope {
  THIS_OCCURRENCE,
  THIS_AND_FUTURE,
  SERIES
}
