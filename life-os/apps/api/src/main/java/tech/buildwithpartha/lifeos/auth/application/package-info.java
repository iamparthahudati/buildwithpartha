/**
 * Identity use cases that orchestrate {@code auth.domain} policy and ports: password validation and
 * hashing, signup (LOS-0503), email verification (LOS-0504), login/session issuance (LOS-0505),
 * logout/session revocation (LOS-0506), and — soon — password reset (LOS-0507).
 */
package tech.buildwithpartha.lifeos.auth.application;
