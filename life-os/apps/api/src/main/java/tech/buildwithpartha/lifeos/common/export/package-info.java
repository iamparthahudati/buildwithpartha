/**
 * Cross-domain export file port and contracts (LOS-1405).
 *
 * <p>Other domains (e.g. {@code auth} or export job handlers) import only this package to create
 * and manage export files, preserving domain boundary rules.
 */
package tech.buildwithpartha.lifeos.common.export;
