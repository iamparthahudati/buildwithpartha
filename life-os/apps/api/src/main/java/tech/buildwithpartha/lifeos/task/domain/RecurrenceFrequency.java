package tech.buildwithpartha.lifeos.task.domain;

/** Frequency / cadence mode for a recurring task series. */
public enum RecurrenceFrequency {
  DAILY,
  WEEKLY,
  MONTHLY,
  WEEKDAY,
  INTERVAL,
  AFTER_COMPLETION
}
