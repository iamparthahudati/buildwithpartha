/**
 * Background job domain: persistent async job queue, worker, and handler framework (LOS-1403).
 * Cross-domain access is through {@code common.job.BackgroundJobPort} only.
 */
package tech.buildwithpartha.lifeos.job;
