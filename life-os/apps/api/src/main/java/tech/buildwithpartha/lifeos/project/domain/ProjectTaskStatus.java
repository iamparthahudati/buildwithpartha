package tech.buildwithpartha.lifeos.project.domain;

/** Status enum for tasks used within the project domain progress and health calculations. */
public enum ProjectTaskStatus {
  TO_DO,
  IN_PROGRESS,
  BLOCKED,
  DONE,
  CANCELLED
}
