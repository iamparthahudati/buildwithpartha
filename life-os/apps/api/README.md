# LifeOS API

Reserved for the Java 21 + Spring Boot API. Bootstrap only through the assigned Epic 02 ticket. Public production base path is `/life-os/api/v1`; the service remains private behind Caddy.

