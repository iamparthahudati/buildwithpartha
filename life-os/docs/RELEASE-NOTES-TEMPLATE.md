# LifeOS vX.Y.Z

- Release ticket:
- Production tag: `life-os-vX.Y.Z`
- Source commit:
- Deployment date/time (UTC):
- Previous production tag:

## Outcome

What changed for the user and why this release exists.

## Added, changed, fixed, security

List ticket IDs with concise outcomes. Do not copy raw commit logs.

## Database migrations

List migration IDs, compatibility window, expected duration/locks and verification. Write “None” when applicable.

## Configuration and infrastructure

List new/changed variables, secrets, resources, routes, scheduled jobs and provider settings without secret values.

## Pre-deployment evidence

- CI/build/test/security/a11y/performance results:
- Staging smoke result:
- Backup/restore readiness:
- Known accepted risks:

## Deployment steps

Reference the runbook and release-specific ordering. Production deploys the exact tagged artifact only.

## Smoke checks

List non-destructive public, auth, private workflow, API, job, metric and alert checks.

## Rollback

State the exact application tag and database/config compatibility steps. A rollback plan cannot assume destructive migration reversal.

## Post-deployment observation

Record error/latency/job/backup/user-journey checks, observation window and final status.

