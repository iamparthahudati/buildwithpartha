# LOS-0616 handoff

- Status: Done
- Branch: `feature/LOS-0616-shell-today-foundation-gate`
- Date: 2026-08-20

## Completed behavior

Ran and passed the private shell/Today foundation gate over LOS-0601–LOS-0615.

- Added an end-to-end backend gate suite proving anonymous and unverified Accounts cannot read Today, verified Accounts receive a local date exactly derived from the response instant in their confirmed IANA timezone, every foundation provider stays zero-safe, and private response data contains no sample record titles.
- Strengthened session authentication so an active session authenticates only while its owning Account exists and is `ACTIVE`. This blocks stale or malformed sessions belonging to unverified, suspended, pending-deletion or deleted Accounts.
- Corrected Today caching to emit the documented `Cache-Control: private, no-store, max-age=0, must-revalidate` policy rather than the previous reduced header.
- Corrected `SecurityControllerTests` to seed verified Accounts; those fixtures had previously created `UNVERIFIED` Accounts and passed only because the filter did not enforce lifecycle status.
- Revalidated protected frontend routing, shell/navigation landmarks and focus, shared Quick Add ownership, idle/restored Focus behavior, Today foundation/populated/partial/offline states, accessibility, responsive contracts and production build.
- Applied the locked Java formatter to the stacked backend state so the global Spotless gate passes without an exception.

## Files and contracts changed

- Updated `ApiSecurityConfiguration` and `SessionAuthenticationFilter` to resolve the owning Account and require `AccountStatus.ACTIVE` before authenticating a session.
- Updated `TodayController` to send the exact private no-store response policy.
- Added `ShellTodayFoundationGateIntegrationTests` and corrected the verified Account setup in `SecurityControllerTests`.
- Mechanically formatted the backend files flagged by the existing Spotless configuration; no behavior changed in those formatting-only files.
- Added `docs/gates/SHELL-TODAY-FOUNDATION-GATE.md` and updated the current-status and backlog ledgers.

## Validation performed

- Full frontend `npm test` passed: 165 test files, 1,503 Vitest tests, formatting, zero-warning lint, strict typecheck, boundaries, token validation, coverage, test-mode production build and 35 Node assertions.
- Full backend `./gradlew check` passed: 425 tests plus Spotless, Checkstyle, architecture and JaCoCo coverage gates.
- Focused backend gate/security suites passed after the Account-status enforcement change.
- `node scripts/validate-docs.mjs` passed.
- The prerequisite live-browser evidence for shell/Today responsive states is consolidated in the gate report. The in-app browser control surface was unavailable for a fresh pass in this session; LOS-0616 introduced no frontend visual changes.

## Database/config/deployment changes

None. The Account-status lookup uses the existing user repository and schema. No environment variable, migration, service or deployment change is required.

## Known limitations or follow-ups

- Full real-data Today providers remain intentionally deferred to LOS-1415; foundation providers continue returning typed empty data.
- Later domain tickets own Task/MIT, Time Block/Focus and Brain Dump mutations. Today keeps those actions disabled or preserves input without claiming persistence.

## Next recommended ticket

LOS-0701 (`docs/backlog/EPIC-07-PROJECTS.md`) — model Projects and Milestones with ownership, lifecycle, priority, health, deadline and versioning invariants.
