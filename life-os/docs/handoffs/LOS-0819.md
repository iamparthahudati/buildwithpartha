# LOS-0819 handoff

- Status: Done
- Branch/commit: `feature/LOS-0819-short-kebab-name` / `feat(tasks): LOS-0819 aggregate Task details`
- Date: 2026-08-23

## Completed behavior

- Added authenticated `GET /tasks/{id}/detail` returning the canonical Task metadata, ordered Subtasks, blocker/dependent projections, related-section counts, and an explicit top-level optimistic-concurrency `version` matching `task.version`.
- Kept missing, deleted, and cross-user Task IDs indistinguishable as `404 RESOURCE_NOT_FOUND`.
- Added a user-scoped dependency read port and JPA adapter that loads blockers and dependents with two fixed projection queries. Query count remains constant as the number of dependency edges grows, and an invalid cross-user edge is not projected.
- Added stable zero-count fields for linked Time Blocks, Focus Sessions, Comments, Attachments, and Activity Events. Their canonical persistence capabilities are introduced by later tickets; confirmed Task `spentMinutes` remains available in Task metadata.
- Documented the aggregate/version contract in the API conventions and exposed the endpoint and schemas in the authenticated OpenAPI 3.1 document.

## Files and contracts changed

- API: `TaskController.java`, `TaskDetailResponse.java`, `TaskDetailCountsResponse.java`, and dependency response mapping overloads.
- Application/domain: `TaskDetailService.java`, `TaskDetailResult.java`, `TaskDetailCounts.java`, `TaskDetailQueryRepository.java`, `TaskDetailDependencies.java`, and `TaskDependencySummaryItem.java`.
- Persistence adapter: `JpaTaskDetailQueryRepository.java`.
- Tests: `TaskDetailControllerTests.java` and Task detail assertions in `OpenApiArtifactTests.java`.
- Documentation: `docs/05-API-CONVENTIONS.md`, `docs/CURRENT-STATUS.md`, and `docs/backlog/STATUS.md`.
- Generated OpenAPI artifact: `apps/api/build/openapi/life-os-openapi.json` (build output, intentionally ignored by Git).

## Validation performed

- Temurin JDK 21.0.11: `./gradlew check --rerun-tasks` passed with all 11 gate tasks executed.
- 613 backend tests passed with no failures, errors, or skips.
- Focused contract/security coverage passed for aggregate success shape, matching version fields, unauthenticated access, cross-user Task isolation, invalid cross-user dependency isolation, missing Task behavior, and OpenAPI schemas/responses.
- Bounded-query coverage passed with the same prepared-statement count for zero dependency edges and 24 dependency edges.

## Database/config/deployment changes

- No database migration, runtime configuration, dependency, or deployment change.

## Known limitations or follow-ups

- Linked Time Block, Focus Session, Comment, Attachment, and Activity Event counts remain zero until their canonical backend persistence tickets are complete. The response contract is ready for those providers without embedding their independently loaded collections.
- No frontend integration was added; that belongs to LOS-0820.

## Next recommended ticket

LOS-0820 — Integrate Task Details.
