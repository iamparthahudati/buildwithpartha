# LOS-0801 handoff

- Status: Done
- Branch/commit: `feature/LOS-0801-model-tasks-and-subtasks`
- Date: 2026-08-21

## Completed behavior

- Modeled core Task and Subtask domain aggregates, persistence entities, repositories, and validation invariants.
- Modeled support for project-linked and projectless tasks, with `ON DELETE SET NULL` on `project_id` foreign key.
- Implemented `TaskStatus` (`TO_DO`, `IN_PROGRESS`, `BLOCKED`, `DONE`, `CANCELLED`) and `TaskPriority` (`P1`, `P2`, `P3`, `P4`) matching LifeOS product vocabulary.
- Modeled soft-deletion (`deleted_at`), archiving (`archived_at`), and `isOverdue` status evaluation against terminal, archived, and deleted states.
- Created partial unique index `uq_tasks_user_mit_date` on `(user_id, mit_date) WHERE mit_date IS NOT NULL AND deleted_at IS NULL` to enforce at most one active Most Important Task (MIT) per user per local date.
- Modeled ordered subtasks within a parent task with completion status and position tracking.
- Reconciled calculation-facing `ProjectTask` contract (from `LOS-0705`) with `ProjectTask.fromStatusName(...)` and `Task.toProjectTask()`.

## Files and contracts changed

- Migration: Added `V9__tasks_and_subtasks_schema.sql` defining `tasks` and `subtasks` tables, constraints, indexes, and comments.
- Test Schema: Added `tasks` and `subtasks` table definitions to `src/test/resources/schema.sql`.
- Domain: Added `Task`, `Subtask`, `TaskStatus`, `TaskPriority`, and `TaskRepository` in `tech.buildwithpartha.lifeos.task.domain`.
- Infrastructure: Added `TaskEntity`, `SubtaskEntity`, `TaskJpaRepository`, `SubtaskJpaRepository`, and `JpaTaskRepository` in `tech.buildwithpartha.lifeos.task.infrastructure`.
- Contract Reconciliation: Updated `ProjectTask` in `tech.buildwithpartha.lifeos.project.domain` with `fromStatusName(...)`.
- Test Suites: Added `TaskDomainTests`, `JpaTaskRepositoryTests`, `TaskSchemaIT`, and updated `TaskDomainFixture`.

## Validation performed

- `./gradlew check` passed 100% clean, including Spotless formatting, Checkstyle rules, ArchUnit package boundary rules, JaCoCo coverage verification, and all 530+ unit/integration tests.

## Database/config/deployment changes

- Flyway migration `V9__tasks_and_subtasks_schema.sql` creates `tasks` and `subtasks` tables, constraints, indexes, and partial unique index for MIT.

## Next recommended ticket

LOS-0802 — Implement task CRUD API.
