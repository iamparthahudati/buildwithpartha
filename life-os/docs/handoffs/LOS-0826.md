# LOS-0826 handoff

- Status: Done (backend + frontend read path + in-app assign/unassign control complete)
- Branch: `feature/LOS-0826-link-tasks-to-milestones`
- Date: 2026-08-30

## Goal

Let a task belong to a project milestone so work can be grouped and viewed by phase/milestone,
instead of the label-only workaround. Closes the gap where `tasks` and `milestones` shared no link.

## Design decision — isolated assignment table

The `Task` aggregate record is constructed in 55 places across 18 files (15 test files). Adding a
`milestone_id` component to that record would ripple through every call site and a large share of the
test suite. Instead this ticket models the link as a **separate one-to-one assignment table**
(`public.task_milestones`, PK `task_id`), mirroring how `task_dependencies` is modeled. The `Task`
aggregate is untouched. Trade-off: reads that need a task's milestone make a second lookup rather
than reading a column; acceptable for the current call volume and keeps blast radius minimal.

Invariants (enforced in `TaskMilestoneService`, not the DB):

- The task and the milestone must belong to the **same project**.
- Both must be owned by the **same user**.
- A projectless task cannot be assigned a milestone.
- One milestone per task (enforced by the `task_id` primary key).
- Deleting the task or the milestone clears the link (`ON DELETE CASCADE`).

## Backend (complete, all tests green)

- Migration `V28__task_milestone_assignment_schema.sql` (+ matching table in test `schema.sql`).
- Domain: `TaskMilestoneAssignment`, `TaskMilestoneRepository` (task module).
- Infrastructure: `TaskMilestoneEntity`, `TaskMilestoneJpaRepository`, `JpaTaskMilestoneRepository`.
- Cross-module port: `common/project/MilestoneLookupPort` + `MilestoneReference`, implemented by
  `project/application/DefaultMilestoneLookupPort` (validates ownership via project → user).
- Application: `TaskMilestoneService` (assign / clear / getAssignedMilestone / tasksForMilestone),
  `MilestoneTaskView`.
- API (`TaskMilestoneController`):
  - `PUT /tasks/{taskId}/milestone` — assign (body `{ milestoneId }`).
  - `GET /tasks/{taskId}/milestone` — current assignment (`{ milestone: null }` when unassigned).
  - `DELETE /tasks/{taskId}/milestone` — clear (204).
  - `GET /milestones/{milestoneId}/tasks` — tasks assigned to a milestone.
- Tests: `TaskMilestoneServiceTests` (assign happy path, milestone-not-found, project mismatch,
  projectless task, task-not-found, clear, list, get). Spotless + checkstyle + full `test` green.

## Frontend

Read path (complete, wired, `verify:quality` + vitest green):

- `projects/model/milestoneTask.ts` — `MilestoneTaskSummary`, `TasksByMilestone`.
- `projects/api/milestoneTasksApi.ts` — `getMilestoneTasks(milestoneId)` (GET `/milestones/{id}/tasks`).
- `projects/hooks/useMilestoneTasks.ts` — `useQueries` over milestone ids → tasks keyed by milestone.
- `ProjectTimeline` renders each milestone's assigned tasks with a count + total-hours summary
  (new optional `tasksByMilestone` prop; additive, existing tests unchanged).
- Threaded through `ProjectDetailsScreen` → `ProjectDetailsRoute` (route fetches per-milestone tasks
  from the project-detail aggregate's milestone ids).
- Tests: `milestoneTasksApi.test.ts`, `taskMilestoneApi.test.ts`, and a `ProjectTimeline` render case.

Write path (complete, `verify:quality` + vitest green):

- `tasks/api/taskMilestoneApi.ts` — `assignTaskMilestone`, `clearTaskMilestone`, `getTaskMilestone`
  (exported from the tasks barrel).
- `projects/hooks/useMilestoneTaskAssignment.ts` — assign/unassign React Query mutations that
  invalidate the per-milestone task lists so the Timeline grouping refreshes.
- `ProjectTimeline` milestone cards now carry an "Add task" menu (candidates = the project's tasks
  not already on that milestone) and a per-task remove button; both gated on `isInteractive`
  (hidden for archived/read-only). Threaded through `ProjectDetailsScreen` → `ProjectDetailsRoute`.
- Contained entirely to the projects feature + route — the fragile `TaskForm` / `TaskDetailsScreen`
  contracts were left untouched. Candidates are same-project by construction (the route supplies only
  this project's tasks); the backend still enforces `MILESTONE_PROJECT_MISMATCH` defensively.
- Tests: `ProjectTimeline` assign/unassign case, plus the API tests.

## Notes for the next contributor

- The milestone picker must filter to the task's own project; assigning cross-project returns
  `400` with field code `MILESTONE_PROJECT_MISMATCH`. Surface that inline on the picker.
- A task with no project cannot take a milestone — hide/disable the picker until a project is set.
