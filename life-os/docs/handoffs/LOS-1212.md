# LOS-1212 handoff

- Status: Done
- Branch: `feature/LOS-1212-compose-integrate-habits`
- Date: 2026-08-30

## Completed behavior

- Replaced the Habits placeholders with authenticated list and detail routes at `/life-os/app/habits` and `/life-os/app/habits/:habitId`.
- Composed Today, all-Habits, archived, overview, history, and statistics views from the LOS-1211 controlled components. The screens expose responsive cards/rows, a real heatmap table alternative, lifecycle dialogs, and loading/empty/recoverable-error/conflict states.
- Made the selected local entry date, list view, detail tab, and inclusive history `from`/`to` range URL-owned. Invalid dates and ranges are normalized; history is capped at the API's 366-day contract.
- Uses the Account timezone for list defaults and the Habit's IANA timezone for direct-detail defaults. Copy and form guidance state that a timezone change affects new entries only and never re-buckets stored local dates.
- Added the complete Habit REST client and TanStack Query layer for list/detail, dated entries, pauses, statistics, create/edit, archive/restore, and pause/resume.
- Entry increments/decrements use absolute set/remove writes instead of retry-sensitive browser increments. Every cached range for the Habit is patched optimistically, the exact pre-mutation snapshots are restored on error, and the canonical entry/statistics plus Today and Activity query families are invalidated after settlement.
- Wired create/edit optimistic versions and explicit 409 recovery, pause/resume, archive confirmation, and archived restoration through the existing LOS-1211 dialogs and actions.
- Extended `GET /habits/{id}/stats` with the authoritative LOS-1210 cadence-aware streak projection: current/longest streak, eligible/met-target periods, and cadence completion rate. The backend loads owner-scoped entries and pauses and excludes fully paused cadence periods without duplicating streak rules in the browser.

## Files and contracts changed

- `apps/api/src/main/java/tech/buildwithpartha/lifeos/habit/application/HabitService.java`, `HabitStatistics.java`: calculate and carry the authoritative streak result alongside raw day-window history counts.
- `apps/api/src/main/java/tech/buildwithpartha/lifeos/habit/api/HabitStatsResponse.java`, `HabitController.java`: expose and document the cadence-aware streak fields on the existing bounded statistics endpoint.
- `apps/web/src/features/habits/api/habitsApi.ts`, `hooks/useHabits.ts`: Habit REST mapping, query keys, lifecycle mutations, precise invalidation, and optimistic dated-entry rollback.
- `apps/web/src/features/habits/components/HabitsScreen.tsx`, `HabitDetailsScreen.tsx`, `habits-screen.css`: controlled responsive Today/list/detail/history/statistics composition.
- `apps/web/src/routes/HabitsRoute.tsx`, `routes/index.ts`, `app/AppRouter.tsx`: authenticated URL state, route orchestration, mutations, dialogs, and real route registration.
- Focused API, hook, screen, and route tests cover authoritative statistics mapping, local-date request contracts, optimistic success/remove/rollback, URL restoration, tab/range state, behavior, and axe accessibility.

## Validation performed

- `npx vitest run src/routes/HabitsRoute.test.tsx src/features/habits` — 27 tests across 10 files pass.
- `npm run verify:quality` — Prettier, zero-warning ESLint, strict TypeScript, module boundaries, and design-token enforcement pass.
- `npm run build:test` — production-mode test build passes; Vite retains the existing informational large-chunk warning.
- `node --test tests/*.test.mjs` — all 35 gateway/build/catalog/foundation/boundary/token assertions pass.
- `./gradlew test --tests '*Habit*' --tests 'tech.buildwithpartha.lifeos.config.OpenApiArtifactTests' --rerun-tasks` — focused Habit and OpenAPI suites pass with zero failures or skips.
- Full frontend `npm test` retains the same unrelated `ProjectsRoute.test.tsx` page-size expectation mismatch documented by LOS-1211; the LOS-1212 focused suite and every frontend quality/build/structural step pass.
- Full backend `./gradlew test --rerun-tasks` runs 1,018 tests: 1,016 pass and the same two unrelated Today/Brain Dump expectations fail (`TodayControllerTests`, `ShellTodayFoundationGateIntegrationTests`).
- Whole-tree Spotless/Checkstyle continues to report only the pre-existing Brain Dump violations documented by LOS-1208/LOS-1209; no LOS-1212 Java file is flagged. `git diff --check` passes.

## Database/config/deployment changes

None. LOS-1212 adds no migration, dependency, browser storage, secret, environment value, provider, or deployment change. The existing authenticated, CSRF-protected same-origin Habit endpoints remain in place.

## Known limitations or follow-ups

- The Habits list currently composes dated entries and pause state from the existing per-Habit endpoints. A future bulk day projection may reduce request fan-out for Accounts with unusually large Habit lists without changing the screen contract.
- Reminder preferences are stored and editable, but actual delivery remains behind the existing notification/privacy gates; the UI does not request permission or imply delivery.
- The global Today widgets and Quick Add integration remain LOS-1213. LOS-1212 already invalidates the Today family precisely so that integration can consume fresh Habit state.
- The unrelated Projects frontend assertion, two Today/Brain Dump backend expectations, and Brain Dump formatting/checkstyle debt remain owned by their documented follow-ups.

## Next recommended ticket

`LOS-1213 — Add Today capture/habit integration`: connect Quick Add and Today Habit widgets to the shared Habit services with accurate queued/saved state and precise invalidation.
