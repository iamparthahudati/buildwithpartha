# LOS-0813 handoff

- Status: Done
- Branch: `feature/LOS-0813-integrate-tasks-screen`
- Date: 2026-08-21

## Completed behavior

- Wired the composed Tasks screen to `GET /tasks` (list + summary), Task CRUD/status/MIT mutations, and `POST /tasks/bulk-actions`.
- Mounted `TasksRoute` at `/life-os/app/tasks`. URL search parameters own tab/status/overdue/archived, search, priority, Project, sort, page, view, and selected Task. Filter changes batch into one navigation so a page reset cannot wipe the new filters.
- Complete, status, archive, and MIT updates use safe optimistic cache patches with rollback on failure. Bulk actions wait for the server so partial item failures can keep failed Tasks selected and name each reason.
- Create/edit dialogs close only after a successful save. A 409 shows the TaskForm conflict reload path and refetches the list.

## Files and contracts changed

- API client: `apps/web/src/features/tasks/api/tasksApi.ts` maps `TaskResponse` and bulk outcomes, and calls list/summary/CRUD/MIT/bulk/label endpoints.
- Query/mutation hooks: `apps/web/src/features/tasks/hooks/useTasks.ts`, `useTaskMutations.ts`.
- Route: `apps/web/src/routes/TasksRoute.tsx` mounted from `AppRouter.tsx`; `tasks/:taskId` stays Coming Soon.
- `TasksScreen` no longer issues a second page-reset navigation alongside filter/tab callbacks, so controlled URL state stays coherent.
- Public exports: `apps/web/src/features/tasks/index.ts`, `apps/web/src/routes/index.ts`.
- Tests: `tasksApi.test.ts`, `useTasks.test.tsx`, `TasksRoute.test.tsx`.

## Validation performed

- Focused frontend tests passed: 82 tests across the Task feature and `TasksRoute`.
- `eslint` (0 warnings) and `tsc --noEmit` passed for the changed files.
- `prettier --write` applied to the changed files.

## Database/config/deployment changes

- None. This ticket adds no persistence, environment values, or deployment changes.

## Known limitations or follow-ups

- Comment counts and blocker counts remain 0 until later Task Details tickets map those APIs.
- `GET /tasks` cannot filter projectless Tasks, so the "No project" filter does not send `projectId`.
- Task title links still point at `/life-os/app/tasks/:taskId`, which remains Coming Soon until Task Details composition/integration.
- Project names are joined from `useProjects`; a missing Project id falls back to the label "Project".

## Next recommended ticket

LOS-0814 — Build TaskDetailsHeader and metadata.
