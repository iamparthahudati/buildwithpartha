# LOS-0707 handoff

- Status: Completed
- Branch/commit: `feature/LOS-0701-model-projects-milestones` / (to be committed)
- Date: 2026-08-20

## Completed behavior

- Built the responsive `ProjectForm` component wrapped in `FormDialog` for creating and editing projects.
- Supported all project fields: Name, Description, Status, Priority, Health, Theme Color & Icon (`ColorIconPicker`), Start & Deadline Dates (`DateInput`), Estimate (`DurationField`), and Labels (`Combobox`).
- Added progressive disclosure for optional/advanced fields with automatic expansion when initial advanced values exist or in edit mode.
- Implemented client-side validation enforcing required name and deadline-after-start-date constraints with accessible `FormErrorSummary` focus management.
- Implemented dirty state tracking to guard against accidental loss of unsaved changes.
- Added version conflict error presentation and resolution trigger (`onResolveConflict`) for optimistic concurrency safety.
- Added catalog specimens and unit test coverage.

## Files and contracts changed

- Added `ProjectForm.tsx`, `project-form.css`, and `ProjectForm.test.tsx` under `src/features/projects/components/`.
- Exported `ProjectForm`, `ProjectFormProps`, and `ProjectFormData` from `@features/projects` (`index.ts`).
- Registered `ProjectFormDemo` and catalog entry in `ComposedDemos.tsx` and `composedEntries.tsx`.

## Validation performed

- `npx vitest run src/features/projects` passed: 21 tests across 3 files.
- `ProjectForm` unit tests verify default create mode, edit mode pre-population, progressive disclosure toggle, field validation, submission formatting, and conflict handling.

## Database/config/deployment changes

None.

## Known limitations or follow-ups

- `ProjectForm` will be wired into `ProjectsScreen` in LOS-0709 and live API mutations in LOS-0710.

## Next recommended ticket

LOS-0708 — Build project summary metric strip.
