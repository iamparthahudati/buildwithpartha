# LOS-1407 handoff

- Status: Done
- Branch/commit: `feature/LOS-1407-standardize-pagination-filter-sort`
- Date: 2026-09-05

## Completed behavior

- Implemented shared, validated pagination and sort primitives (`tech.buildwithpartha.lifeos.common.pagination`) conforming to `docs/backlog/EPIC-14-BACKEND-OPERATIONS.md`:
  - `PaginationParams`: Immutable value object record enforcing bounds (`page >= 0`, `1 <= size <= 100`) and validating `sortDirection` ("ASC" or "DESC").
  - `PaginationUtils`: Shared utility methods for page, size, and sort field validation throwing standard `FieldValidationException` with field `page`, `size`, `sortBy`, `sortDirection` and problem code `INVALID`.
  - `applyStableSort`: CriteriaQuery ordering helper appending primary sort field and stable secondary `id` tie-breaker to ensure deterministic page ordering.
- Refactored pagination parameter validation across backend domain controllers and application services:
  - `TaskController` (`/tasks` query)
  - `ProjectController` (`/projects` query)
  - `NoteController` (`/notes` query)
  - `GoalController` (`/goals` query & `/goals/{goalId}/check-ins`)
  - `BrainDumpController` (`/brain-dump-items` query)
  - `CommentService` (Task & Project comments listing)
  - `ActivityReadService` (Product activity listing)
  - `NotificationController` (`/notifications` listing)
- Added stable secondary tie-breaker (`ORDER BY n.createdAt DESC, n.id DESC`) to `NotificationJpaRepository`.

## Files and contracts changed

- `apps/api/src/main/java/tech/buildwithpartha/lifeos/common/pagination/PaginationParams.java` — New immutable validated record.
- `apps/api/src/main/java/tech/buildwithpartha/lifeos/common/pagination/PaginationUtils.java` — New shared utility class with `DEFAULT_PAGE`, `DEFAULT_PAGE_SIZE`, `MAX_PAGE_SIZE`, `validate(...)`, `validatePageAndSize(...)`, and `applyStableSort(...)`.
- `apps/api/src/main/java/tech/buildwithpartha/lifeos/task/api/TaskController.java` — Refactored validation to use `PaginationUtils`.
- `apps/api/src/main/java/tech/buildwithpartha/lifeos/project/api/ProjectController.java` — Refactored validation to use `PaginationUtils`.
- `apps/api/src/main/java/tech/buildwithpartha/lifeos/note/api/NoteController.java` — Refactored validation to use `PaginationUtils`.
- `apps/api/src/main/java/tech/buildwithpartha/lifeos/goal/api/GoalController.java` — Refactored validation to use `PaginationUtils`.
- `apps/api/src/main/java/tech/buildwithpartha/lifeos/braindump/api/BrainDumpController.java` — Refactored validation to use `PaginationUtils`.
- `apps/api/src/main/java/tech/buildwithpartha/lifeos/comment/application/CommentService.java` — Aligned page/size validation with `PaginationUtils`.
- `apps/api/src/main/java/tech/buildwithpartha/lifeos/audit/application/ActivityReadService.java` — Aligned page/size validation with `PaginationUtils`.
- `apps/api/src/main/java/tech/buildwithpartha/lifeos/notification/api/NotificationController.java` — Added page/size parameter validation.
- `apps/api/src/main/java/tech/buildwithpartha/lifeos/notification/infrastructure/NotificationJpaRepository.java` — Updated query with stable secondary tie-breaker.
- `apps/api/src/test/java/tech/buildwithpartha/lifeos/common/pagination/PaginationUtilsTests.java` — Unit tests for pagination primitives.

## Validation performed

- `./gradlew check` passed in `apps/api` with zero violations across Spotless, Checkstyle, ArchUnit, unit/integration tests, JaCoCo thresholds (>= 80%), and OpenAPI spec checks.
- `npm run verify:quality` passed in `apps/web` with zero formatting, linting, type, boundary, or design token violations.

## Next recommended ticket

LOS-1408 — Add idempotency infrastructure.
