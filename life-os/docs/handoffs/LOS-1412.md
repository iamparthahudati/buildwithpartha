# LOS-1412 handoff

- Status: Done
- Branch/commit: `feature/LOS-1412-add-metrics-and-alerts-contract`
- Date: 2026-09-10

## Completed behavior

- Implemented backend Micrometer metrics instrumentation and published the documented metrics and alerts contract (`docs/44-METRICS-AND-ALERTS-CONTRACT.md`):
  - `MetricsService` (`tech.buildwithpartha.lifeos.common.metrics`) central Spring service providing typed Micrometer instruments for Auth, Background Jobs, HTTP API requests, HTTP Caching, Mail, Private Exports, and Business Health gauges with tag value sanitization (`sanitizeTag`).
  - `ApiMetricsFilter` (`tech.buildwithpartha.lifeos.common.metrics`) Servlet filter recording HTTP API request volume (`lifeos.api.requests`) and latency (`lifeos.api.request.duration`) with privacy-safe, low-cardinality `status_class` (`2xx`, `3xx`, `4xx`, `5xx`), `method`, and normalized `uri_template` tags.
  - Instrumented `LoginService` recording login attempts (`lifeos.auth.login.attempts`) tagged with `result` and `reason`.
  - Instrumented `BackgroundJobWorker` recording job executions (`lifeos.job.executions`) and execution durations (`lifeos.job.duration`) tagged by `kind` and `status`.
  - Instrumented `DataExportJobHandler` recording export generation count (`lifeos.export.generations`), execution duration (`lifeos.export.duration`), and archive size bytes (`lifeos.export.bytes`).
  - Configured Spring Boot Actuator endpoints in `application.yml` and `ApiSecurityConfiguration` exposing `/actuator/prometheus` and `/actuator/metrics` endpoints under internal security rules.
  - Authored `docs/44-METRICS-AND-ALERTS-CONTRACT.md` documenting metric inventory, tag privacy rules, scrape endpoints, and alert threshold definitions (Warning/Critical) with evaluation windows and operational runbook steps.
  - Added test suite (`MetricsContractTests`) verifying Micrometer metric registrations, counter/timer/gauge recordings, and tag privacy sanitization.

## Files and contracts changed

- `apps/api/src/main/java/tech/buildwithpartha/lifeos/common/metrics/MetricsService.java` — Central Micrometer metrics instrument bean with tag privacy sanitization.
- `apps/api/src/main/java/tech/buildwithpartha/lifeos/common/metrics/ApiMetricsFilter.java` — Servlet filter recording HTTP API request volume and latency.
- `apps/api/src/main/java/tech/buildwithpartha/lifeos/auth/application/LoginService.java` — Instrumented login attempt metrics.
- `apps/api/src/main/java/tech/buildwithpartha/lifeos/job/application/BackgroundJobWorker.java` — Instrumented job execution and duration metrics.
- `apps/api/src/main/java/tech/buildwithpartha/lifeos/export/application/DataExportJobHandler.java` — Instrumented data export metrics.
- `apps/api/src/main/java/tech/buildwithpartha/lifeos/config/ApiSecurityConfiguration.java` — Permitted prometheus and metrics actuator endpoints.
- `apps/api/src/main/resources/application.yml` — Exposed prometheus and metrics actuator endpoints.
- `apps/api/src/test/java/tech/buildwithpartha/lifeos/common/metrics/MetricsContractTests.java` — Unit and integration test suite for metrics contract.
- `docs/44-METRICS-AND-ALERTS-CONTRACT.md` — Metrics and alerts specification document.
- `docs/backlog/EPIC-14-BACKEND-OPERATIONS.md` — Marked LOS-1412 as Done.
- `docs/CURRENT-STATUS.md` — Updated current status with LOS-1412 completion.

## Validation performed

- `./gradlew spotlessCheck checkstyleMain checkstyleTest` passed with 0 errors or warnings.
- `./gradlew test` passed 100% (1,280+ tests passing).

## Next recommended ticket

LOS-1413 — Tune database queries and indexes.
