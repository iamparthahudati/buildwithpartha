# LOS-0812 handoff

- Status: Done
- Branch: `feature/LOS-0812-compose-tasks-screen`
- Date: 2026-08-21

## Completed behavior

- Composed `TasksScreen` from completed Task components: PageHeader with Add task, TaskSummaryMetrics, status/overdue/archived tabs, SearchField, priority/Project filters, SortControl, ViewToggle, DataTable with selection, TaskCard/TaskRow responsive views, bulk actions, pagination, TaskForm, confirm dialogs, and a list-context DetailPanel.
- Mock list covers MIT, overdue, blocked, done, cancelled, archived, projectless, and ordinary open Tasks. Ready, first-use empty, search/filtered empty, loading, error/retry, and partial bulk-failure states are all present.
- Bulk bar appears only with a selection and exposes Status, Priority, Project, Labels, Schedule, Clear schedule, Archive, and Clear selection. Partial failures keep failed Tasks selected and name each exact reason.
- Tab/preset helpers reuse the LOS-0811 URL keys (`status`, `overdue`) and add `archived=true` for the Archived view, preserving unrelated filters and resetting pagination.

## Files and contracts changed

- Screen composition: `apps/web/src/features/tasks/components/TasksScreen.tsx`, `tasks-screen.css`, `TaskBulkActions.tsx`.
- Screen model: `TaskRecord` plus `toTaskListItem` in `model/task.ts`; filter/sort/summary/bulk/URL helpers in `model/taskScreen.ts`; fixtures in `model/mockTasks.ts`.
- Public exports: `apps/web/src/features/tasks/index.ts`.
- Catalog: `apps/web/src/catalog/TasksScreenDemos.tsx` and `composedEntries.tsx`.
- Verification: focused screen, helper, and catalog registry tests.

## Validation performed

- Focused frontend tests passed: 83 tests across the Task feature and catalog registry, including axe coverage on the populated Tasks screen.
- `eslint` (0 warnings) and `tsc --noEmit` passed for the changed files.
- `git diff --check` passed.

## Database/config/deployment changes

- None. This is a mocked screen-composition ticket. It adds no API calls, persistence, browser storage, environment values, or deployment changes.

## Known limitations or follow-ups

- List data, summary counts, CRUD, MIT, bulk mutations, and URL search parameters remain caller-controlled. LOS-0813 wires the Task list/summary/CRUD/MIT/bulk APIs and React Router state into these contracts.
- Comment counts and blocker counts are fixture/projection fields until comments and dependency APIs are integrated on Task Details.
- Task title links still point at `/life-os/app/tasks/:taskId`, which remains a coming-soon route until Task Details composition.

## Next recommended ticket

LOS-0813 — Integrate the Tasks screen.
