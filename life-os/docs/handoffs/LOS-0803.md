# LOS-0803 handoff

- Status: Done
- Branch/commit: `feature/LOS-0803-implement-task-query-api`
- Date: 2026-08-21

## Completed behavior

- Added authenticated task list query REST API (`GET /tasks`) with text search (`q`), filtering by project ID, statuses, priorities, MIT date, MIT flag, overdue state, archived state, and due date range.
- Added user-scoped task summary metrics endpoint (`GET /tasks/summary-counts`) returning counts for `total`, `toDo`, `inProgress`, `blocked`, `done`, `cancelled`, `overdue`, and `mit`.
- Implemented bounded pagination (size 1-100), allowlisted sort fields (`title`, `status`, `priority`, `dueAt`, `createdAt`, `updatedAt`, `position`), sort direction (`ASC`/`DESC`), and a primary key tie-breaker (`id`) for stable sorting.
- Modeled `TaskQuery`, `TaskQueryResult`, and `TaskSummaryCounts` in the `tech.buildwithpartha.lifeos.task.domain` package adhering to ArchUnit inward-dependency rules.
- Implemented JPA Criteria API queries and native SQL summary counts in `JpaTaskRepository`.

## Files and contracts changed

- Domain: Added `TaskQuery`, `TaskQueryResult`, `TaskSummaryCounts` in `tech.buildwithpartha.lifeos.task.domain`, and updated `TaskRepository`.
- Infrastructure: Implemented criteria-based `queryTasks` and native `getSummaryCounts` in `JpaTaskRepository`.
- Application & API: Updated `TaskService` and added `GET /tasks` and `GET /tasks/summary-counts` in `TaskController`.
- Response DTOs: Added `TaskQueryResponse`.
- Test Suites: Added `TaskQueryTests` and updated `JpaTaskRepositoryTests`.

## Validation performed

- `./gradlew check` passed 100% clean, including Spotless formatting, Checkstyle rules, ArchUnit boundary rules, JaCoCo coverage verification, and all 540 tests.

## Next recommended ticket

LOS-0804 — Implement subtask API.
