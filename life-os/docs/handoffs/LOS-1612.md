# LOS-1612 handoff

- Status: Completed
- Branch/commit: `feature/LOS-1612-deployment-rollback-runbooks`
- Date: 2026-09-12

## Completed behavior

- **Authored Deployment and Rollback Runbooks Specification (`life-os/docs/60-DEPLOYMENT-AND-ROLLBACK-RUNBOOKS.md`)**:
  - Defined end-to-end standard operating procedures (SOPs), disaster recovery runbooks, emergency access, and incident escalation protocols for LifeOS across staging (`staging.buildwithpartha.tech`) and production (`buildwithpartha.tech`).
  - **Target environment safety framework (§1)**: Established mandatory target validation matrix (`TARGET_ENV=staging|production`) checking domain, host (`srv1883798.hstgr.cloud`), compose file, secrets file permissions (`0600`), container names (`lifeos-prod-api` vs `lifeos-staging-api`), and volume paths prior to any mutating command.
  - **Normal deployment runbook (§2)**: Documented pre-deployment checklists, staging candidate validation, release approval gate for tagged `master`, pre-rollout Flyway database migrations, zero-downtime rolling container updates, health check verification, and deployment audit logging.
  - **Failed migration rollback & recovery runbook (§3)**: Documented triage and diagnosis commands for `flyway_schema_history`, migration lock handling, Flyway repair procedures (`flyway repair` / schema history cleanups), partial DDL rollback protocols, and database restoration fallbacks.
  - **Bad application rollout (bad app) rollback runbook (§4)**: Documented rapid container rollback procedures reverting to previous known-good immutable tags (`lifeos-web:<prev>`, `lifeos-api:<prev>`), zero-downtime container recreation, health probe verification, and error-rate stabilization checks.
  - **Database disaster recovery & restore runbook (§5)**: Integrated `restore-postgres-backup.sh` (from LOS-1608) with AES-256 GPG decryption using vault-secured passphrases, SHA-256 checksum verification, RPO ($24\text{ h}$) and RTO ($< 15\text{ min}$) adherence, and post-restoration account deletion ledger replay for DPDP/GDPR compliance (ADR-012).
  - **Application files disaster recovery & restore runbook (§6)**: Integrated `restore-app-files-backup.sh` (from LOS-1609) enforcing strict DB-first, files-second restore ordering and post-restore orphan reconciliation.
  - **Cloudflare bypass & emergency origin access runbook (§7)**: Documented Hostinger direct SSH access, Serial Web Console fallback, direct origin loopback health checks (`127.0.0.1:8443`), Cloudflare "Pause Cloudflare on Site" / "Under Attack Mode" toggles, and UFW emergency firewall adjustments.
  - **TLS certificate renewal & emergency replacement runbook (§8)**: Documented Cloudflare Origin CA certificate creation, staging `/etc/ssl/lifeos/origin.pem` and `.key` (`0644`/`0600`), zero-downtime Caddy reload (`caddy reload`), and TLS handshake validation.
  - **Routine & emergency secrets rotation runbooks (§9)**: Defined 90/180/365-day rotation cycles and emergency credential compromise procedures for `POSTGRES_PASSWORD`, `JWT_SECRET`, `CSRF_SECRET`, `CLOUDFLARE_API_TOKEN`, `BACKUP_ENCRYPTION_PASSPHRASE`, and SSH keys, enforcing non-logging validation.
  - **Incident management, escalation matrix & contact paths (§10)**: Defined P0–P3 severity classifications, response/resolution SLAs (P0 $< 15\text{ m}$, P1 $< 30\text{ m}$, P2 $< 2\text{ h}$), primary/secondary contact channels, and a 48-hour Post-Incident Review (PIR) template.

- **Created Release Rollback Script (`life-os/scripts/rollback-release.sh`)**:
  - Implemented automated rollback script supporting `--target=staging|production`, `--tag=<sha-or-version>`, `--dry-run`, and `--skip-smoke`.
  - Performs target safety checks on environment parameters, Compose files, and container definitions.
  - Resolves previous release tag automatically from `/var/log/life-os/deployments.json` if not provided.
  - Performs rolling container update via Docker Compose and executes post-rollback health checks (`/life-os/` and `/life-os/api/v1/actuator/health`).
  - Appends secret-safe rollback audit records to `deployments.json` with status `ROLLBACK_SUCCESS`.
  - Marked executable (`chmod +x`).

- **Created Automated Verification Script (`life-os/scripts/validate-deployment-runbooks.sh`)**:
  - Created 11-section automated audit suite validating specification completeness, normal deploy execution, failed migration repair, bad app rollback script, database/file restore integration, Cloudflare bypass, TLS renewal, secrets rotation, incident escalation matrix, and documentation ledgers.
  - Marked executable (`chmod +x`).

## Files and contracts changed

### Specifications & Runbooks
- [NEW] [`60-DEPLOYMENT-AND-ROLLBACK-RUNBOOKS.md`](file:///Users/parthahudati/Workspace/Website/buildwithpartha/life-os/docs/60-DEPLOYMENT-AND-ROLLBACK-RUNBOOKS.md) — Comprehensive operational runbooks covering normal deployments, failed migrations, bad app rollbacks, DB/file restores, Cloudflare bypass, TLS certificate renewals, secrets rotation, and incident escalation paths.

### Scripts & Automation
- [NEW] [`scripts/rollback-release.sh`](file:///Users/parthahudati/Workspace/Website/buildwithpartha/life-os/scripts/rollback-release.sh) — Executable rollback automation script with target checks, health probes, and audit logging.
- [NEW] [`scripts/validate-deployment-runbooks.sh`](file:///Users/parthahudati/Workspace/Website/buildwithpartha/life-os/scripts/validate-deployment-runbooks.sh) — Automated verification suite for deployment and rollback runbooks (11 sections, 0 failures).

### Documentation & Backlog
- [MODIFY] [`docs/backlog/EPIC-16-INFRA-LAUNCH.md`](file:///Users/parthahudati/Workspace/Website/buildwithpartha/life-os/docs/backlog/EPIC-16-INFRA-LAUNCH.md) — Updated LOS-1612 status to `Done`.
- [MODIFY] [`docs/backlog/STATUS.md`](file:///Users/parthahudati/Workspace/Website/buildwithpartha/life-os/docs/backlog/STATUS.md) — Added LOS-1610, LOS-1611, and LOS-1612 entries; updated last updated date.
- [MODIFY] [`docs/CURRENT-STATUS.md`](file:///Users/parthahudati/Workspace/Website/buildwithpartha/life-os/docs/CURRENT-STATUS.md) — Added LOS-1612 summary to Completed section.
- [NEW] [`docs/handoffs/LOS-1612.md`](file:///Users/parthahudati/Workspace/Website/buildwithpartha/life-os/docs/handoffs/LOS-1612.md) — This handoff document.

## Validation performed

- Executed `sh life-os/scripts/validate-deployment-runbooks.sh --dry-run` — all 11 sections passed with 0 failures.
- Executed `sh life-os/scripts/rollback-release.sh --target=staging --dry-run` — passed cleanly.
- Executed `sh life-os/scripts/rollback-release.sh --target=production --tag=v0.9.9 --dry-run` — passed cleanly.
- Executed `node life-os/scripts/validate-docs.mjs` — verified all Markdown links and ticket status ledger.
- Executed `sh life-os/scripts/validate-deployment-pipeline.sh --dry-run` — verified deployment pipeline assertions.
- Executed `sh life-os/scripts/validate-centralized-logs.sh --dry-run` — verified centralized logs assertions.
- Executed `sh life-os/scripts/validate-monitoring-and-alerting.sh --dry-run` — verified monitoring assertions.

## Database/config/deployment changes

- **No database schema changes**.
- **Operational automation added**: `rollback-release.sh` is ready for operator use on staging and production VPS.
- **No new secret variables required**: Uses existing secrets file structure (`/etc/life-os/secrets/.env.production` and `.staging`).

## Known limitations or follow-ups

- **Zero-downtime Blue/Green deployment** is not implemented in v1 single-VPS Compose setup; rolling restart takes $\approx 2\text{--}5\text{ seconds}$ during which Caddy proxies to the healthy backend or returns standard retry-friendly responses. Blue/Green or multi-node deployment is a post-launch enhancement.
- **Automated canary analysis** is not configured in v1; rollbacks are operator-triggered or scripted via `rollback-release.sh`.

## Next recommended ticket

- **LOS-1613**: Rehearse release and rollback (Deploy a release candidate to staging, migrate, smoke, roll back app, test compatible DB strategy, and record timings/gaps).
