# LOS-0820 handoff

- Status: Done
- Branch/commit: `feature/LOS-0820-short-kebab-name` / included in ticket commit
- Date: 2026-08-23

## Completed behavior

- Added a query-backed Task Details integration that maps `GET /tasks/{id}/detail` into the composed screen and keeps that aggregate as the sole client-side domain copy.
- Wired versioned Task editing and lifecycle actions: complete/reopen, MIT toggle, duplicate, archive/restore, and delete. Successful mutations invalidate the shared Task query family so list and detail views converge on server state.
- Wired Subtask add, edit, toggle, reorder, and delete plus dependency search, add, remove, and completion navigation through the existing Task mutation contracts.
- Connected Schedule and Start Focus actions to the canonical Time Blocks and Focus routes with the Task ID, avoiding a second scheduling or focus state model inside Task Details.
- Reused the LOS-0819 aggregate counts for Comments, Attachments, and Activity. Attachments remain privacy-gated, while the later persistence/integration tickets retain ownership of their collections and mutations.
- Added refresh-safe `/tasks/:taskId` deep links, URL-backed selected tabs, validated exact-list `returnTo` handling, deletion return, and a URL-backed Task Details sheet that preserves the mounted Tasks list and its filters.

## Files and contracts changed

- API mapping: `features/tasks/api/tasksApi.ts` now exposes the Task detail aggregate and Subtask/dependency mutation contracts.
- Query/mutations: `useTaskDetail.ts`, `useTaskDetailMutations.ts`, shared Task query keys, and public Task feature exports.
- Composition: `IntegratedTaskDetails.tsx` connects the aggregate and mutations to `TaskDetailsScreen.tsx` without copying domain state.
- Routing: `TaskDetailsRoute.tsx`, `TasksRoute.tsx`, `AppRouter.tsx`, and route exports provide direct and list-context entry points.
- Tests: API mapping, query behavior, integrated composition, direct route, composed screen, and list-context route coverage.

## Validation performed

- Focused frontend suite passed: 6 files and 25 tests covering Task detail mapping/querying, integration mutations, composed screen behavior, direct deep links, and list-context sheets.
- `npm test` passed in `apps/web`: formatting, lint, type checking, boundaries, token checks, 209 Vitest files with 1,772 tests, coverage thresholds, production test build, and all 35 Node checks.
- Live browser verification passed at 320px, 768px, and 1440px: responsive page/sheet layout, intended tab-strip overflow only, keyboard tab navigation, Escape close with trigger focus restoration, and no warning/error console entries.
- Backend code was not changed, so `./gradlew check` was not applicable.

## Database/config/deployment changes

- No database migration, runtime configuration, dependency, backend, or deployment change.

## Known limitations or follow-ups

- Comment persistence and UI integration remain LOS-0821/LOS-0822; Activity persistence and UI integration remain LOS-0823/LOS-0824. LOS-0820 consumes their stable aggregate counts without inventing duplicate collections.
- Attachments remain disabled behind the existing privacy gate until a canonical private-file capability is connected.
- The Time Blocks and Focus destinations own their domain state. LOS-0820 passes Task context into those shared routes and does not add separate schedule/focus persistence.

## Next recommended ticket

LOS-0821 — Implement task comments.
