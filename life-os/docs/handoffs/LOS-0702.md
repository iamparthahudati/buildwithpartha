# LOS-0702 handoff

- Status: In Review
- Branch/commit: `feature/LOS-0701-model-projects-milestones` / `de7e644`
- Date: 2026-08-20

## Completed behavior

- Added authenticated project create, read, update, archive, restore, and delete endpoints under `/projects`.
- Added validated request/response DTOs and defaults for project status, priority, and health.
- Enforced user ownership for every project lookup and label ownership for create/update operations; cross-user resources return not found.
- Added optimistic-version conflict handling with standard `409` problem details.
- Made archive and restore idempotent, preserved immutable domain updates, and emitted lifecycle audit events.
- Kept all mutating routes behind the existing session and CSRF protections.

## Files and contracts changed

- Added `project.api` CRUD requests, `ProjectResponse`, and `ProjectController`.
- Added project application commands and `ProjectService`.
- Added shared label-ownership validation and concurrency-conflict problem handling.
- Extended `Project` with update, archive, and restore transitions.
- Added authenticated integration coverage in `ProjectControllerTests`.

## Validation performed

- `./gradlew check` passed, including API tests, formatting, style, architecture boundaries, and JaCoCo coverage gates.
- CRUD tests cover authentication/CSRF, validation, ownership, label isolation, optimistic concurrency, archive/restore, and delete behavior.

## Database/config/deployment changes

None. LOS-0702 uses the LOS-0701 schema and adds no migration, environment variable, or deployment change.

## Known limitations or follow-ups

- The work is committed on the existing LOS-0701 feature branch rather than a ticket-specific LOS-0702 branch; review and merge should preserve the individual ticket commit.

## Next recommended ticket

LOS-0703 — Implement the project list query API.
