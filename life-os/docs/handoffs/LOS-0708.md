# LOS-0708 handoff

- Status: Completed
- Branch/commit: `feature/LOS-0701-model-projects-milestones` / (to be committed)
- Date: 2026-08-20

## Completed behavior

- Built `ProjectSummaryMetrics` component on top of `MetricCard` (LOS-0419).
- Displays summary statistics for Total Projects, Active Projects, Completed Projects, On Hold Projects, At Risk / Off Track Projects, and Average Progress percentage.
- Supports interactive filter actions on individual metric cards to trigger filter callbacks (`onSelectFilter`).
- Fully supports ready, loading, empty, and error/retry states with accessible live region announcements and high contrast focus states.
- Added catalog specimens and automated unit tests.

## Files and contracts changed

- Added `ProjectSummaryMetrics.tsx`, `project-summary-metrics.css`, and `ProjectSummaryMetrics.test.tsx` under `src/features/projects/components/`.
- Exported `ProjectSummaryMetrics`, `ProjectSummaryMetricsProps`, `ProjectSummaryCounts`, and `ProjectFilterCategory` from `@features/projects` (`index.ts`).
- Registered `ProjectSummaryMetricsDemo` and catalog entry in `ComposedDemos.tsx` and `composedEntries.tsx`.

## Validation performed

- `npx vitest run src/features/projects` passed: 25 tests across 4 files.
- Unit tests verify metric calculation presentation, loading state, error and retry behavior, and filter action selection.

## Database/config/deployment changes

None.

## Known limitations or follow-ups

- Will be composed into `ProjectsScreen` in LOS-0709.

## Next recommended ticket

LOS-0709 — Compose Projects screen with mock states.
