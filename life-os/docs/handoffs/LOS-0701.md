# LOS-0701 handoff

- Status: In Review
- Branch: `feature/LOS-0701-model-projects-milestones`
- Commits: `0f502c2`, `20ff270`
- Date: 2026-08-20

## Completed behavior

Successfully modeled the core projects and milestones domain aggregates and their relational representations in public schema.

- Added a forward-only Flyway migration (`V8__projects_and_milestones_schema.sql`) creating tables for `labels`, `projects`, `project_labels`, and `milestones`.
- Modeled the core domain aggregates (`Project`, `Milestone`, `Label`) as immutable Java records with validation rules covering priority, health, statuses, estimate values, and start/deadline validations (e.g. deadline cannot precede start date).
- Configured JPA persistence entities (`ProjectEntity`, `MilestoneEntity`, `LabelEntity`) and Spring Data JpaRepository adapters (`JpaProjectRepository`, `JpaMilestoneRepository`, `JpaLabelRepository`).
- Configured element collection mappings for linking labels on `ProjectEntity` to align with DDD aggregate boundaries.
- Adjusted ArchUnit configuration rules in `PackageBoundaryRules.java` to whitelist the newly introduced `label` package directory.
- Appended H2 tables to the H2 test `schema.sql` to support full JPA validation during test profile bootstrap.

## Files and contracts changed

- Added Flyway migration: `V8__projects_and_milestones_schema.sql`
- Appended H2 schema setup: `src/test/resources/schema.sql`
- Modified ArchUnit rules: `src/test/java/tech/buildwithpartha/lifeos/architecture/PackageBoundaryRules.java`
- Added Label domain and JPA persistence:
  - `tech.buildwithpartha.lifeos.label.domain.Label`
  - `tech.buildwithpartha.lifeos.label.domain.LabelRepository`
  - `tech.buildwithpartha.lifeos.label.infrastructure.LabelEntity`
  - `tech.buildwithpartha.lifeos.label.infrastructure.LabelJpaRepository`
  - `tech.buildwithpartha.lifeos.label.infrastructure.JpaLabelRepository`
  - `tech.buildwithpartha.lifeos.label.package-info.java`
- Added Project & Milestone domain and JPA persistence:
  - `tech.buildwithpartha.lifeos.project.domain.Project`
  - `tech.buildwithpartha.lifeos.project.domain.ProjectStatus`
  - `tech.buildwithpartha.lifeos.project.domain.ProjectPriority`
  - `tech.buildwithpartha.lifeos.project.domain.ProjectHealth`
  - `tech.buildwithpartha.lifeos.project.domain.ProjectRepository`
  - `tech.buildwithpartha.lifeos.project.domain.Milestone`
  - `tech.buildwithpartha.lifeos.project.domain.MilestoneStatus`
  - `tech.buildwithpartha.lifeos.project.domain.MilestoneRepository`
  - `tech.buildwithpartha.lifeos.project.infrastructure.ProjectEntity`
  - `tech.buildwithpartha.lifeos.project.infrastructure.ProjectJpaRepository`
  - `tech.buildwithpartha.lifeos.project.infrastructure.JpaProjectRepository`
  - `tech.buildwithpartha.lifeos.project.infrastructure.MilestoneEntity`
  - `tech.buildwithpartha.lifeos.project.infrastructure.MilestoneJpaRepository`
  - `tech.buildwithpartha.lifeos.project.infrastructure.JpaMilestoneRepository`

## Validation performed

- Database integration tests (`ProjectSchemaIT.java`) run against local PostgreSQL 16.14, verifying schema, cascading deletion (on user deletion and project deletion), unique normalized names, and check constraints (priority, status, health, deadline date validation). All passed.
- Unit/slice tests (`ProjectTests.java`, `LabelTests.java`) verify domain constraints and construction logic. Infrastructure database tests (`JpaProjectRepositoryTests.java`, `JpaMilestoneRepositoryTests.java`, `JpaLabelRepositoryTests.java`) are implemented as `@DataJpaTest`s running against H2, verifying entity mapping round-trips, `@ElementCollection` update behavior, Spring Data queries, native query summary counts, and pagination/filtering criteria query logic. All tests passed.
- Running `./gradlew check` verifies formatting, style, boundaries, and JaCoCo coverage (exceeding the strict 80% line/branch ratio limit) successfully.

## Database/config/deployment changes

- New Flyway schema migration `V8__projects_and_milestones_schema.sql` adding four new tables. Standard application roles (`lifeos_migrator` / `lifeos_app`) acquire permissions automatically. No new config files or environment variables.

## Known limitations or follow-ups

None.

## Next recommended ticket

LOS-0702 (`EPIC-07-PROJECTS.md`) — Implement project CRUD API.
