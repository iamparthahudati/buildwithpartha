# LOS-0519 handoff

- Status: Done
- Branch: `develop`
- Date: 2026-08-19

## Completed behavior

Implemented self-service privacy and data settings UI conforming to `02-ARCHITECTURE.md`, `05-API-CONVENTIONS.md`, `06-SECURITY.md`, `31-PRIVACY-DATA-LIFECYCLE.md`, and `docs/backlog/EPIC-05-IDENTITY.md`:

- **Privacy & Data API & Hooks**:
  - `privacyApi.ts` in `features/settings/api`: Typed `apiRequest` endpoints for `listExports`, `requestDataExport`, and `deleteAccountApi`.
  - `useDataExports` / `useRequestDataExport` in `features/settings/hooks`: TanStack Query integration for fetching active export history and requesting new background export archives with cache invalidation.
  - `useAccountDeletion` in `features/settings/hooks`: Mutation hook for submitting verified account deletion requests with re-authentication credentials and confirmation phrase.
- **PrivacySettingsPanel (`features/settings/components`)**:
  - **Data Export Section**: Explains archive contents (portable JSON profile, preferences, activity, terms audit history). Displays live list of past/active exports with status badges (`READY`, `GENERATING`, `EXPIRED`), file size, and download link for ready packages. Includes "Request new export archive" action.
  - **Privacy Policy & Posture Summary**: Transparent adult-first privacy statement (no ad targeting, no secret leaks, no data selling).
  - **Account Deletion Section (Danger Zone)**: Highlights irreversible data removal consequences. Opens destructive confirmation modal requiring the user's exact display name and current account password before the delete action is enabled.
  - **SettingsScreen Integration**: Registered `PrivacySettingsPanel` as the default panel for the "Data & privacy" tab in `SettingsScreen`.
- **Styling**:
  - Added export history and danger zone CSS definitions in `settings-screen.css`.

## Validation performed

- Frontend Quality Gates:
  - `npm run test:coverage && npm run build:test`: Unit tests, 90.54% statement / 88.4% branch coverage, TypeScript compilation, and production Vite build passed with 0 errors.
- Backend Quality Gates:
  - `./gradlew check -x spotlessCheck`: All checkstyle, ArchUnit architecture rules, Spring Boot integration tests, and JaCoCo coverage (>= 80%) passed with `BUILD SUCCESSFUL`.
  - `bash scripts/verify-flyway-postgres.sh`: Migration and PostgreSQL schema validation passed cleanly.
- Tests Added/Updated:
  - `apps/web/src/features/settings/components/PrivacySettingsPanel.test.tsx` (new comprehensive test suite)
  - `apps/web/src/features/settings/components/SettingsScreen.test.tsx` (verified data tab rendering)

## Next recommended ticket

LOS-0520 (EPIC-05 Run identity threat-model and security gate).
