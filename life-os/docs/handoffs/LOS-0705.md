# LOS-0705 handoff

- Status: In Review (blocked on LOS-0801 contract reconciliation)
- Branch/commit: `feature/LOS-0701-model-projects-milestones` / `0612b39`
- Date: 2026-08-20

## Completed behavior

- Added count-based and estimate-weighted project progress policies.
- Excluded archived and cancelled tasks from progress and health calculations.
- Defined honest rounding: partial progress is clamped to `1`–`99`, with exact zero and completion producing `0` and `100`.
- Preserved explicit manual health; otherwise derived active-project health from overdue and blocked tasks.
- Returned `NOT_SET` health for completed/cancelled projects, inactive projects without manual health, zero-task projects, and other non-derivable states.

## Files and contracts changed

- Added `ProgressPolicy`, `ProjectProgress`, `ProjectProgressCalculator`, `ProjectTask`, and `ProjectTaskStatus` domain types.
- Added focused unit coverage for zero-task, mixed-state, rounding, weighting, archived/cancelled, overdue, blocked, and manual-health cases.

## Validation performed

- `./gradlew check` passed, including `ProjectProgressCalculatorTests`, style checks, and coverage gates.

## Database/config/deployment changes

None. This is a pure domain calculation with no persistence or configuration change.

## Known limitations or follow-ups

- LOS-0801 is not yet implemented. `ProjectTask` is the calculation-facing contract created by this ticket and must be reconciled with the canonical Task aggregate/API when LOS-0801 lands before LOS-0705 can be considered unblocked and merged as Done.
- The work is committed on the existing LOS-0701 feature branch; preserve the ticket commit during review/merge.

## Next recommended ticket

LOS-0801 — Model tasks and subtasks, then reconcile its task contract with `ProjectTask`.
