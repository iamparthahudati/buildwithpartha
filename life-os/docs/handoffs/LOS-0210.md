# LOS-0210 handoff

- Status: Done
- Branch: `feature/LOS-0210-local-full-stack-gateway`
- Date: 2026-08-16

## Completed behavior

Established Vite as the local same-origin gateway for both development and preview. The browser remains on `http://localhost:5173`; only the complete `/life-os/api` path segment is forwarded to Spring Boot at `http://127.0.0.1:8080`, and the path/query are not rewritten.

All other `/life-os/*` requests remain with the frontend SPA. A live-server contract test proves API routing occurs before fallback, upstream success/error responses remain API responses, nested UI refreshes return the LifeOS document, and a similarly named path such as `/life-os/apiary` remains a UI route.

## Files and contracts changed

- Added one shared regular-expression proxy rule to Vite development and preview configuration.
- Added `npm run verify:gateway` and a disposable Vite/mock-upstream routing test using random loopback ports.
- Documented the three-terminal PostgreSQL/API/web startup flow and the relative browser API rule.
- Recorded the local port contract: browser/Vite `5173`, Spring Boot upstream `8080`, preview `4173`, PostgreSQL `55432`.
- Added no package, runtime service, CORS permission, API endpoint, database object or production Caddy configuration.

## Validation performed

- `npm run verify:gateway`: pass; two gateway contract tests.
- `npm test`: pass; formatting, zero-warning lint, strict types, module boundaries, component coverage, production build and nine Node contract tests pass with zero skips.
- Backend `./gradlew clean build`: pass on JDK 21.
- Cross-stack dependency lock validation: pass.
- LifeOS documentation validation: pass.
- Ticket diff and whitespace/error-marker check: pass.

## Database/config/deployment changes

- Local Vite development/preview configuration only.
- No dependency or lockfile change.
- No runtime environment value, browser storage, backend behavior, database, migration, provider, container, VPS, Cloudflare or production deployment change.

## Known limitations or follow-ups

- Docker remains unavailable in the current environment, so the actual PostgreSQL-backed API was not started. The routing contract is exercised end to end through real Vite and a disposable loopback upstream; no test is skipped.
- The current API has no product endpoint yet. Health/Problem Details and OpenAPI arrive in LOS-0213 and LOS-0214.
- Production/staging Caddy routing remains owned by LOS-1604 and must preserve the same API-before-SPA ordering.

## Next recommended ticket

`LOS-0211 — Define environment validation`.
