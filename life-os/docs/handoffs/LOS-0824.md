# LOS-0824 handoff

- Status: Done
- Branch/commit: `feature/LOS-0824-short-kebab-name` / this ticket commit
- Date: 2026-08-23

## Completed behavior

- Added one shared frontend Activity API/query boundary for bounded zero-based `GET /tasks/{id}/activity` and `GET /projects/{id}/activity` pages; detail surfaces convert those pages to the shared component's one-based pagination contract.
- Wired Task Details Activity and both the Project Details overview/recent feed and dedicated Activity tab to live LOS-0823 data. Current Account display name is used for the actor slot; the API's current owner-scoped object label/link is preserved, while `object: null` continues through ActivityFeed's safe non-linked deleted-item fallback.
- Mapped every closed LOS-0823 Activity Event type to calm past-tense action copy, an Activity type category, and a decorative icon. Accessible native-select filtering covers Project, Task, Subtask, and Comment changes while retaining an All activity option.
- Reused ActivityFeed's visible relative time plus full localized absolute `aria-label`/`title`, and retained timezone-aware day grouping.
- Invalidates all loaded Activity pages after successful Task create/update/status/lifecycle/duplicate/bulk/MIT operations, Subtask and dependency mutations, Project mutations, and Comment create/edit/delete. Failed mutations do not claim or refresh a new Activity Event.
- Added interactive Task and Project Details catalog specimens for filters, pagination, responsive layout, and timestamps; ProjectDetailsScreen is now directly selectable in the development-only catalog.

## Files and contracts changed

- `apps/web/src/features/activity/**` — shared typed API DTOs, bounded query keys/hook, event presentation/filter mapping, accessible Activity type control, and focused tests.
- `apps/web/src/features/tasks/components/{IntegratedTaskDetails,TaskDetailsScreen}.tsx` — live Task Activity query, filtering, status/retry, pagination, and presentation wiring.
- `apps/web/src/routes/ProjectDetailsRoute.tsx` and `apps/web/src/features/projects/components/{ProjectDetailsScreen,ProjectOverview}.tsx` — live Project Activity query plus overview/dedicated-tab composition.
- Task, Project, Subtask/dependency, and Comment mutation hooks now invalidate the shared Activity query boundary after successful writes.
- Focused Task/Project route, screen, mutation, API, mapping, filter, deleted-object, pagination, and accessible timestamp tests were added or updated. Development catalog specimens cover both detail surfaces.

## Validation performed

- Focused Activity/Task/Project/Comment integration suite passes: 12 files / 58 tests, plus the updated development catalog test (13 assertions).
- `npm test` passes from `apps/web`: formatting, lint, strict TypeScript, module boundaries, design-token enforcement, 214 Vitest files / 1,795 tests, coverage thresholds, production test build, and all 35 Node assertions.
- Coverage passes at 84.37% statements, 81.55% branches, 80.48% functions, and 85.66% lines. The new Activity API and presentation modules have 100% statement/function/line coverage.
- Live development-catalog verification passes at 320 × 820, 768 × 900, and 1440 × 900. Task and Project feeds have no page-level horizontal overflow; filtering, one-based pagination controls, current-object links, relative/absolute timestamp pairing, and responsive wrapping are visible and operable.
- Keyboard verification confirms the tab pattern reaches/selects Activity with End and retains a visible 2px focus outline; the native filter exposes its persistent visible label and focus outline. Browser console verification found no warnings or errors.

## Database/config/deployment changes

- None. No endpoint, schema, Flyway migration, environment value, dependency, secret, browser persistence, processor, or deployment topology changed.

## Known limitations or follow-ups

- LOS-0823 exposes pagination but no server-side event-type filter parameter. The type control therefore narrows the current bounded server page, and its filtered empty copy explicitly says “on this page”; pagination remains newest-first across all Activity types.
- Activity remains content-minimized: it does not reconstruct historical titles or field-level before/after values. Deleted/unavailable objects use the shared safe fallback by design.

## Next recommended ticket

LOS-0825 — run the Tasks phase gate.
