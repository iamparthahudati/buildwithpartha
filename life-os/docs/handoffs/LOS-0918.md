# LOS-0918 handoff

- Status: Done
- Branch/commit: `feature/LOS-0918-scheduling-focus-gate` / `feat(time): LOS-0918 pass scheduling and focus gate`
- Date: 2026-08-25

## Completed behavior

- Passed the Epic 09 phase gate and recorded acceptance evidence in `docs/gates/SCHEDULING-FOCUS-PHASE-GATE.md`.
- Completed the canonical Task scheduling handoff: `/time-blocks?taskId=...` opens Create Time Block only after the owner-scoped Task option is available and selects that Task without duplicating Task state.
- Integrated overlap preflight into Time Block create and edit. A conflict keeps the form open, names the conflicting Time Blocks, and exposes the explicit “Allow scheduling despite conflict” choice. The final write still preserves the backend's race-safe conflict enforcement.
- Keeps create/edit failures in the dialog with an actionable error instead of closing it or producing an unhandled rejected promise.
- Prevents real query failures from falling through to the composed screen's catalog fixtures; an error now shows an empty controlled record set.
- Replaced viewport-based Time Summary breakpoints with container queries and added a compact Upcoming-row container layout. Metric cards, charts, labels, time ranges, and actions no longer collide in the 340–380px desktop sidebar.
- Strengthened cache-refresh tests so Time Block and Focus Session changes explicitly prove Calendar, Today, and daily-report invalidation.
- Stabilized the Focus break-to-focus transition assertion by waiting for the canonical asynchronous query state.

## Files and contracts changed

- Time Blocks route and route tests: Task deep link, overlap preflight/override, dialog error state, and honest failed-query data.
- Time Blocks composed screen: optional deep-linked Task, controlled form pending/error/conflict state, reset lifecycle, and submit continuation result.
- Time Summary/metrics/Upcoming styles: container-responsive column and TimeBlockRow behavior.
- Time Block/Focus hook tests: complete projection-invalidation evidence and deterministic transition observation.
- Documentation: Epic 09 backlog, current status, gate report, and this handoff.

## Validation performed

- Backend Temurin JDK 21.0.11 `./gradlew check --rerun-tasks`: passed with all 11 tasks executed; 759 tests, 0 failures/errors/skips; 96.08% line and 80.31% branch coverage.
- Frontend `npm test`: passed with 237 Vitest files / 1,954 tests and coverage of 84.61% statements, 81.32% branches, 80.13% functions, and 85.92% lines; Prettier, zero-warning ESLint, strict TypeScript, module boundaries, design-token enforcement, production test build, and 35 repository Node assertions also passed.
- Focused route/form/screen/hook suites passed for Task deep links, conflict/override, no-fixture errors, cache invalidation, Focus two-consumer completion, DST form behavior, and axe coverage.
- Rendered development-catalog checks passed at minimum and large specimens. Root/sidebar/Upcoming row `scrollWidth` never exceeded `clientWidth`; summary grids resolved to one container-sized column in the reproduced 380px sidebar.
- Current local backend startup validated all 18 migrations, applied the previously pending V15–V18 migrations to the local PostgreSQL 16.14 database, and exposed the Time Block/report endpoints under `/life-os/api/v1`.
- `node scripts/validate-docs.mjs`: passed with no broken local links.

## Database/config/deployment changes

- No migration, API schema, dependency, environment key, secret, provider, browser storage, privacy lifecycle, deployment unit, or production setting was added or changed.
- The local development database moved from schema version 14 to the already-committed version 18 when the stale Aug 23 backend was restarted. This was local runtime reconciliation, not a LOS-0918 schema change.
- No production deployment was performed.

## Known limitations or follow-ups

- The gate verifies the current single-Account v1 workflow. Shared/team calendars and collaborative focus are outside v1 scope.
- Range/project/category reporting and metric dictionary versioning remain owned by LOS-1105/LOS-1106; they must reuse the LOS-0917 daily calculation.
- The local stack uses PostgreSQL 16.14 because Docker/PostgreSQL 18.4 is unavailable on this Mac, consistent with earlier engineering gate handoffs.

## Next recommended ticket

LOS-1001 — Model and implement Sprints API.
