# LOS-0818 handoff

- Status: Done
- Branch/commit: `feature/LOS-0818-compose-task-details` / included in the ticket commit
- Date: 2026-08-23

## Completed behavior

- Composed the reusable Task Details experience from the existing `TaskDetailsHeader`, `SubtaskChecklist`, `DependencyEditor`, `SchedulingPanel`, comment, attachment, and activity contracts; this ticket adds no API integration.
- Added controlled Details, Subtasks, Dependencies, Comments, Attachments, and Activity tabs with stable selected-tab behavior and accessible count labels. The optional Attachments tab is omitted when its feature gate is disabled, and an unavailable attachment deep link safely resolves to Details.
- Added a responsive list-context `TaskDetailsSheet`: a 480px trailing desktop sheet and full-screen narrow presentation. The originating Task control regains focus when the sheet closes, while the mounted list can retain its exact search, filter, sort, view, page, selection, and scroll state.
- Preserved an exact caller-supplied Tasks return URL in breadcrumbs and recovery actions instead of rebuilding list state from partial parameters.
- Covered initial loading, first-use empty regions, independent region errors, aggregate unavailable and service-error recovery, background refresh with confirmed data, offline confirmed-data read-only behavior, archived read-only behavior with lifecycle actions, known deletion, pending mutations, and partial failures.
- Kept mutations caller-controlled and suppressed them when the aggregate is offline, archived, deleted, or otherwise read-only. Confirmed data remains visible, and no state claims server success before the caller confirms it.
- Added interactive development catalog specimens for populated, empty, partial-failure, refreshing, offline, archived, deleted, loading, unavailable, service-error, and list-context sheet states using neutral mock data only.

## Files and contracts changed

- Task Details composition and responsive presentation: `apps/web/src/features/tasks/components/TaskDetailsScreen.tsx`, `TaskDetailsSheet.tsx`, `task-details-screen.css`, and `task-details-sheet.css`.
- Focused behavior/accessibility coverage: `apps/web/src/features/tasks/components/TaskDetailsScreen.test.tsx` and `TaskDetailsSheet.test.tsx`.
- Public Task feature contract: `TaskDetailsScreen`, `TaskDetailsSheet`, their configuration types, and `TaskDetailsTabId` exported from `apps/web/src/features/tasks/index.ts`.
- Shared Drawer contract: optional `closeLabel` supplies the complete accessible name needed by the Task Details sheet while preserving the existing `Close` default; covered in `apps/web/src/components/feedback/Drawer.test.tsx`.
- Development catalog: `apps/web/src/catalog/TaskDetailsDemos.tsx` and `apps/web/src/catalog/composedEntries.tsx`.
- No route, API, architecture, vocabulary, privacy, storage, or server contract changed.

## Validation performed

- Focused Task Details, Drawer, catalog, and accessibility run passed: 5 files / 95 tests. Coverage includes tab composition and keyboard navigation, exact list-return URLs, comment keyboard submission, optional-attachment gating, every aggregate state, independent region failures, read-only lifecycle behavior, responsive Drawer focus entry/return, and axe audits.
- Full `npm test` from `apps/web` passed: formatting, lint, strict TypeScript, module boundaries, design-token enforcement, 206 Vitest files / 1,762 tests, production coverage thresholds, production test build, and 35 Node assertions.
- Production coverage: 84.96% statements, 82.18% branches, 81.71% functions, and 86.21% lines. `TaskDetailsScreen.tsx` reached 95.23% statements, 83.51% branches, 83.33% functions, and 97.56% lines; `TaskDetailsSheet.tsx` reached 100% statements, functions, and lines.
- Live catalog verification passed at actual 320 × 820, 768 × 900, and 1440 × 900 browser viewports. The page had no horizontal scroll; the six-tab strip retained its own narrow-screen scrolling. The sheet filled 320 × 820 on mobile and occupied the trailing 480 × 900 area on desktop.
- Live keyboard/focus verification confirmed ArrowRight moved selection and focus from Details to Subtasks, opening the sheet focused `Close task details`, and closing returned focus to `Open Prepare weekly review`.
- Live state verification covered partial Subtasks failure with the Task header retained, offline confirmed data, and known deletion without stale tabs. Browser console verification found no warnings or errors.

## Database/config/deployment changes

- None. This ticket adds no migration, endpoint, environment value, browser persistence, deployment change, processor, or new private-data flow.

## Known limitations or follow-ups

- This is a presentation-first mock composition. LOS-0819 owns the bounded Task detail aggregate API, and LOS-0820 owns query/mutation integration and route wiring.
- Comments, attachments, activity, dependency, subtask, scheduling, and focus actions remain controlled callbacks until LOS-0820 connects their approved service boundaries.
- Attachments remain optional and gateable; this catalog enables them only to exercise the completed presentation. No file upload or storage integration is introduced here.
- The older backlog ledger still omits some completed dependency rows even though their handoffs and `CURRENT-STATUS.md` record them as Done. Historical ledger repair is outside this one-ticket scope.

## Next recommended ticket

LOS-0819 — Implement the Task detail aggregate API.
