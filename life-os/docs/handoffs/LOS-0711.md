# LOS-0711 handoff

- Status: Completed
- Branch/commit: `feature/LOS-0711-project-details-header`
- Date: 2026-08-21

## Completed behavior

- Built `ProjectDetailsHeader` component in `apps/web/src/features/projects/components/ProjectDetailsHeader.tsx` with responsive layout, custom stylesheet (`project-details-header.css`), and comprehensive unit test suite (`ProjectDetailsHeader.test.tsx`).
- Implemented deep link breadcrumb navigation using `Breadcrumbs` (LOS-0417), displaying custom breadcrumbs or fallback hierarchy (`Projects` link -> Project name).
- Displayed project identity including colored icon (`getProjectIcon`, `getProjectColor`), title (`Heading` level 1), optional description, and prominent "Archived" badge when `archivedAt` is present.
- Rendered status, health, priority, owner name, start/deadline date range or single date display, and overdue badge indicator when deadline is past for active projects.
- Displayed task completion progress (`completedTasksCount` / `totalTasksCount`) with `ProgressBar` primitive and formatted estimate label.
- Integrated primary action ("Add task" button, hidden in archived state unless overridden) and secondary actions dropdown menu (`Menu`, LOS-0415) supporting Edit, Mark complete, Archive, Restore, Cancel, and Delete project actions.
- Handled `Skeleton` loading placeholders for headers in loading state.
- Registered `ProjectDetailsHeaderDemo` in the composed component catalog (`ComposedDemos.tsx`, `composedEntries.tsx`).

## Files and contracts changed

- Added `ProjectDetailsHeader.tsx`, `project-details-header.css`, and `ProjectDetailsHeader.test.tsx` under `apps/web/src/features/projects/components/`.
- Modified `index.ts` under `apps/web/src/features/projects/` to export `ProjectDetailsHeader` and `ProjectDetailsHeaderProps`.
- Modified `ComposedDemos.tsx` and `composedEntries.tsx` under `apps/web/src/catalog/` to add `ProjectDetailsHeaderDemo` specimen.
- Added `docs/handoffs/LOS-0711.md` and updated `docs/CURRENT-STATUS.md`.

## Validation performed

- `npm run verify:quality` passed: Prettier format check, zero ESLint warnings, TypeScript typecheck (`tsc --noEmit`), module boundaries check, and design tokens check.
- `npm test` passed: 708 unit tests across 114 test files, V8 coverage check, production build, and local gateway tests.

## Database/config/deployment changes

None.

## Known limitations or follow-ups

- Overview components, timeline/milestones, and Project Details screen composition/tabs will be built in subsequent tickets (LOS-0712 – LOS-0716).

## Next recommended ticket

LOS-0712 — Build project overview components.
